{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/RJiazhen/learn-by-diff/main/packages/protocol/schema.json",
  "title": "Learning Course Protocol (LCP) v1",
  "description": "JSON Schema for LearnByDiff course.yml and chapter YAML documents. Wire via YAML language server ($schema comment or yaml.schemas).",
  "oneOf": [{ "$ref": "#/$defs/course" }, { "$ref": "#/$defs/chapter" }],
  "$defs": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "sourceDirPath": {
      "type": "string",
      "minLength": 1,
      "description": "Directory path relative to the source repository root (or source.root). Nested paths allowed; no absolute paths or '..' segments.",
      "pattern": "^(?!/)(?![A-Za-z]:[/\\\\])(?!.*(?:^|[/\\\\])\\.\\.(?:[/\\\\]|$))(?!.*(?:^|[/\\\\])\\.(?:[/\\\\]|$))[^\\s].*$"
    },
    "relativeFilePath": {
      "type": "string",
      "minLength": 1,
      "description": "File path relative to the chapter snapshot tree root (inside fromDir / toDir).",
      "pattern": "\\S"
    },
    "courseSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repository"],
      "properties": {
        "repository": {
          "type": "string",
          "minLength": 1,
          "description": "Git URL or filesystem path to the source repository. Relative paths resolve from the course repository."
        },
        "root": {
          "$ref": "#/$defs/sourceDirPath",
          "description": "Optional subdirectory under repository that prefixes every chapter fromDir / toDir."
        }
      }
    },
    "courseWorkspace": {
      "type": "object",
      "additionalProperties": false,
      "required": ["install", "dev", "test"],
      "properties": {
        "install": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Command run once when creating the learning workspace."
        },
        "dev": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Dev-server / watch command for the learning workspace."
        },
        "test": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Test command for the learning workspace."
        }
      }
    },
    "course": {
      "type": "object",
      "additionalProperties": false,
      "required": ["protocolVersion", "id", "title", "source", "workspace"],
      "properties": {
        "protocolVersion": {
          "type": "integer",
          "const": 1,
          "description": "Learning Course Protocol version. Must be 1."
        },
        "id": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Stable course id (also used as the default learning workspace folder name)."
        },
        "title": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Human-readable course title."
        },
        "source": {
          "$ref": "#/$defs/courseSource"
        },
        "workspace": {
          "$ref": "#/$defs/courseWorkspace"
        }
      }
    },
    "chapter": {
      "type": "object",
      "additionalProperties": false,
      "description": "All fields optional. Defaults: id from filename (strip numeric prefix), title=id, empty fromDir/toDir = empty trees, omitted entryFiles = all files under toDir.",
      "properties": {
        "id": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Stable chapter id (unique within the course). Defaults from the yaml filename."
        },
        "title": {
          "$ref": "#/$defs/nonEmptyString",
          "description": "Human-readable chapter title. Defaults to id."
        },
        "fromDir": {
          "anyOf": [{ "type": "string", "maxLength": 0 }, { "$ref": "#/$defs/sourceDirPath" }],
          "description": "Start snapshot directory under the source repo (or source.root). Empty = empty start tree."
        },
        "toDir": {
          "anyOf": [{ "type": "string", "maxLength": 0 }, { "$ref": "#/$defs/sourceDirPath" }],
          "description": "Goal snapshot directory under the source repo (or source.root). Empty = empty goal tree."
        },
        "entryFiles": {
          "type": "array",
          "uniqueItems": true,
          "description": "Optional paths relative to the chapter tree root. When omitted, all files under toDir are used.",
          "items": {
            "$ref": "#/$defs/relativeFilePath"
          }
        }
      }
    }
  }
}

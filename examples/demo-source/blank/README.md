# Blank

This is a **blank chapter**: Not Started is an empty tree, and Completed is only this README.

It exists to test **http(s) docs**. The docs button on this chapter opens a web URL (`https://book.chibivue.land`), not a file inside the snapshot.

There is no code to implement. Use the docs action to confirm the link opens in the browser.

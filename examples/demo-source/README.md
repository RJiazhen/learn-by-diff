# Demo source (local F5 fixture)

Each chapter is a **directory snapshot**:

- `start/` — hello chapter starts here
- `hello/` — hello goal / world start
- `world/` — world goal / bang start
- `bang/` — bang goal (`hello world!`)

# Example courses

Published Learning Course Protocol sample repositories (`.course-config` only, no source trees) will live here.

They are not npm packages. Protocol unit tests use `packages/protocol/fixtures/` instead.
